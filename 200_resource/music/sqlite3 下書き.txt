sqlite3 下書き

//挿入

insert into GameMusicData( Title , Bpm , time , Hyousi) values ('初詣' , 150 , 30 , 4);
insert into Defficult(Easy, Nomal, Hard, Beyond) values(false, false, false, true);

//更新
update GameMusicData set Bpm = 200 , time = 134 WHERE Title = 'Reverator_in_Vavel' 


//閲覧

select * from GameMusicData;

//列挿入
ALTER TABLE GameMusicData ADD COLUMN HighScore INT DEFAULT 0;

//キー投下
ALTER TABLE GameMusicData
ADD ;

//主キー化
ALTER TABLE GameMusicData ADD PRIMARY KEY(KeyCode);


//作成
create table GameMusicData( MusicKey Integer Not Null PRIMARY KEY AUTOINCREMENT,Title NCHER, Bpm Int, time Int, Hyousi Int, HighScore INT DEFAULT 0);


create table Defficult( MusicKey Intager ,Easy Boolen DEFAULT false, Nomal Boolen DEFAULT true, Hard Boolen DEFAULT false, Beyond Boolen DEFAULT false,FOREIGN KEY (MusicKey) REFERENCES GameMusicData( Musickey));

create table Score( MusicKey Intager ,Easy Int DEFAULT 0, Nomal Int DEFAULT 0, Hard Int DEFAULT 0, Beyond Int DEFAULT 0,FOREIGN KEY (MusicKey) REFERENCES GameMusicData( Musickey));




SELECT * FROM GameMusicData LEFT OUTER JOIN Defficult ON GameMusicData.MusicKey = Defficult.MusicKey;


ALTER TABLE GameMusicData ADD COLUMN KeyCode Int AUTOINCREMENT;

ALTER TABLE GameMusicData ADD COLUMN DffNomal Boolen DEFAULT false;
ALTER TABLE GameMusicData ADD COLUMN DffHard Boolen DEFAULT false;
ALTER TABLE GameMusicData ADD COLUMN DffBeyond Boolen DEFAULT false;
